{
  "name": "single-spa-exercise",
  "version": "1.0.0",
  "description": "single-spa exercise",
  "workspaces": [
    "app/*"
  ],
  "scripts": {
    "start": "lerna run start --stream",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "amir",
  "license": "ISC",
  "dependencies": {
    "lerna": "^8.1.5"
  }
}
